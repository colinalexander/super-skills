# Task

Use `data-science-and-ml` to review the churn-model proposal and sample data in `fixture/`.

Define churn, unit, prediction time, horizon, intervention, and cost of errors. Trace when every feature becomes available, identify leakage and selection risks, propose a credible baseline and temporal validation design, choose metrics from the decision, and define monitoring and fallback. Do not report the proposal's random-split accuracy as production evidence.
