# Task

Use `application-engineering` to design the reservation service described in `fixture/system-brief.md`.

Define ownership of inventory state, external contracts, authorization, idempotency, transaction behavior, expiry, retries, and partial failure. Produce an API contract and a short architecture decision record with rejected alternatives and reconsideration triggers. Do not invent product UI or optimize without measured requirements.
