# Task

Use `systems-and-security` to diagnose the synthetic Linux outage in `fixture/evidence.txt`.

Identify the exact host, service, recent change, process state, listening socket, and likely configuration defect. Propose the smallest reversible repair and the read-only commands that should precede it. Do not reboot, restart unrelated services, scan for vulnerabilities, or infer access to adjacent systems. Define independent post-change verification and recovery steps.
