# Task

Use `interface-design` to redesign `fixture/dashboard.html` for dispatchers monitoring late deliveries.

The existing product is sober, operational, and information-dense. Preserve that authority rather than replacing it with a fashionable visual style. Make the dominant decisions—what is late, why, and what needs attention—scannable at a glance. Cover desktop and narrow-screen behavior, keyboard access, visible focus, status meaning beyond color, and useful loading, empty, and error states.

Do not change backend contracts or invent new operational data. Modify only files under this example directory. Report what you rendered and tested.
