# Requirements

Support agents may inspect any ticket assigned to their team. They may draft and publish internal comments and move tickets only through the transitions allowed by the ticket service. A ticket can change between read and write. Duplicate comments are externally visible and must be prevented. The first release must not delete tickets, change assignees, message customers, or run arbitrary API requests.
