# Task

Use `agent-tooling-and-orchestration` to design the smallest safe tool interface satisfying `fixture/requirements.md`.

Define triggering and non-triggering requests, observable outcomes, required authority, side effects, confirmation boundaries, failure recovery, and representative positive and negative evals. Do not expose a catch-all ticket mutation tool or assume that API reachability grants permission.
