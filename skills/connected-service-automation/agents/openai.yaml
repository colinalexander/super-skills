interface:
  display_name: "Connected-Service Automation"
  short_description: "Operate connected services with safe state changes"
  default_prompt: "Use $connected-service-automation to perform this service operation with explicit targeting, safe mutation, and verification."
policy:
  allow_implicit_invocation: true
